<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <declare-styleable name="BLCountDownProgressCircle">
        <attr name="countDown_circleWidth" format="dimension" />
        <attr name="countDown_centerTextSize" format="dimension" />
        <attr name="countDown_betaAngle" format="integer" />
        <attr name="countDown_firstColor" format="reference|color" />
        <attr name="countDown_secondColor" format="reference|color" />
        <attr name="countDown_centerTextColor" format="reference|color" />
        <attr name="countDown_isShowGradient" format="boolean" />
        <attr name="countDown_text" format="reference|string" />
    </declare-styleable>
</resources>
